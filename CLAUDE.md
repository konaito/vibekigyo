# プロジェクトルール

## ビルドシステム
- このプロジェクトはBunをJavaScriptランタイムおよびパッケージマネージャーとして使用
- `npm`や`yarn`の代わりに`bun`コマンドを使用すること
- 例: `bun install`, `bun run dev`, `bun test`など
- 重要: `bun run dev`は実行しない - ユーザーが常に自分で実行する
- 重要: ビルド/テストコマンドを実行する前に必ず`cd frontend`でfrontendディレクトリに移動すること

## Gitワークフロー
- 新しい作業には常にfeatureブランチを作成
- `feature/feature-name`のような説明的なブランチ名を使用
- mainブランチに直接pushしない
- すべての変更にプルリクエストを作成

## Pushワークフロー
ユーザーが「push」と言った場合の完全なワークフロー:
1. `bun run build`を実行してコードがエラーなくコンパイルされることを確認
2. 説明的な名前で新しいfeatureブランチを作成
3. 意味のあるコミットメッセージですべての変更をステージングしコミット
4. ブランチをリモートにpush
5. `gh pr create`を使用してプルリクエストを作成
6. **プルリクエスト確認**: `open <PR_URL>`コマンドでブラウザでプルリクエストを開く

## 技術的な修正履歴

### OpenAI構造化出力の修正 (2025年1月)
- **問題**: `gpt-4o-search-preview`モデルで「Unterminated string in JSON」エラーが発生
- **原因**: 
  1. JSONスキーマ検証エラー（markdownフィールドがrequired配列に含まれていたが、実際はオプション）
  2. `max_tokens: 2000`によるレスポンスの途中切断
  3. 長い技術仕様書生成時のトークン制限超過

- **解決策**:
  1. **スキーマ修正** (`/lib/openai.ts`): `required: ["type", "message"]` - markdownをオプションに変更
  2. **トークン制限拡大**: `max_tokens: 2000` → `max_tokens: 4000`
  3. **プロンプト改善**: レスポンスサイズに注意する指示を両プロンプトに追加
  4. **TypeScript型修正**: `markdown?: Record<string, string>` - オプション型に変更

### Googleフォーム統合の実装
- **vibeアプリ.md**に「アプリを作成する」ボタンを追加
- 技術仕様書の内容を`formUrl()`関数でGoogleフォームURLにエンコード
- URLの長さ制限チェックを実装（コメントアウト済み）
- エラーハンドリングとコンソールログ出力を追加

### 検索機能の強調
- **検索機能の宣伝**: `gpt-4o-search-preview`モデルの検索機能を両アプリで強調
- **vibe起業.md**: 市場調査、競合分析、投資動向のリアルタイム検索を宣伝
- **vibeアプリ.md**: 最新技術情報、ライブラリ情報、ベストプラクティスの検索を宣伝
- 初期挨拶メッセージに検索機能の説明を追加

### チャットメッセージのマークダウン対応 (2025年1月)
- **目的**: AIアシスタントからのレスポンスをより見やすくするため
- **実装内容**:
  1. **ReactMarkdown導入**: `react-markdown`と`remark-gfm`を使用
  2. **ユーザーメッセージ**: プレーンテキストのまま（`whitespace-pre-wrap`）
  3. **AIメッセージ**: マークダウンレンダリング対応
  4. **カスタムスタイリング**: 
     - 見出し（h1-h3）: サイズと色調整
     - コードブロック: 背景色とボーダー追加
     - インラインコード: 青色背景で強調
     - リスト: 適切なインデントと色調整
     - 引用: 青いボーダーライン

- **技術詳細**:
  - **ファイル**: `/src/components/chat/ChatMessages.tsx`
  - **依存関係**: `react-markdown`, `remark-gfm`, `@tailwindcss/typography`
  - **スタイリング**: Tailwind CSS の `prose` クラス + カスタムコンポーネント
  - **TypeScript対応**: 適切な型定義でESLintエラーを解決

### トークン制限の撤廃 (2025年1月)
- **問題**: `max_tokens: 4000`制限により、長い技術仕様書やビジネス企画書が途中で切断される
- **解決策**:
  1. **トークン制限完全撤廃**: `/lib/openai.ts`から`max_tokens`パラメータを削除
  2. **プロンプト改善**: 「レスポンスサイズに注意」の文言を削除
  3. **詳細重視に変更**: 
     - **技術仕様書**: 「実装に必要な詳細情報を包括的に記載」
     - **ビジネス企画書**: 「事業成功に必要な詳細情報を網羅的に記載」

- **効果**:
  - **完全なレスポンス**: AIが生成する企画書が途中で切断されない
  - **詳細な内容**: より包括的で実用的な企画書・技術仕様書の生成
  - **ユーザー体験向上**: 中途半端な情報ではなく完全な文書を取得可能

### プロンプトの共通化とリファクタリング (2025年1月)
- **目的**: プロンプトの保守性向上とコードの重複排除
- **実装内容**:
  1. **共通部品の抽出**: `/src/lib/prompts/common-parts.ts`を作成
     - `searchCapabilitySection`: 検索機能の共通説明
     - `thoughtProcessSection`: 思考プロセスの共通部分
     - `outputFormatSection`: 出力形式の共通部分
     - `jsonRulesSection`: JSON形式の共通ルール
     - `qualityRulesSection`: 品質ルールの共通部分
  2. **特化部品の分離**:
     - `businessSearchDetails`: ビジネス特化の検索機能詳細
     - `technicalSearchDetails`: 技術特化の検索機能詳細
     - `businessOutputExamples`: ビジネス向け出力例
  3. **プロンプトファイルの簡素化**: 共通部品をインポートして再利用

### ビジネステンプレートの改善 (2025年1月)
- **問題**: 起業企画書で「🛠 技術スタック」が重要度に比べて不適切
- **解決策**: 「👥 チーム・役割」に変更
- **理由**: 事業成功には技術よりもチーム編成と役割分担が重要
- **ファイル**: `/src/app/page.tsx`の`templateSections`

## 現在の技術スタック
- **フロントエンド**: Next.js 15.3.3, React, TypeScript
- **ランタイム**: Bun
- **AIモデル**: OpenRouter経由の`openai/gpt-4o-search-preview`
- **デプロイ**: Vercel (想定)
- **API**: OpenRouter API (構造化出力対応)